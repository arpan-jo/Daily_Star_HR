import { googleWebClientIdd } from '@env';
import IMAGES from './Images';
import { COLORS, SIZES } from './Themes';

export { COLORS, SIZES, IMAGES };

/** OAuth web client id used by every Google sign-in in the app. */
export const GOOGLE_WEB_CLIENT_ID = googleWebClientIdd;
